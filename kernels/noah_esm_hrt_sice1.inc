! HRT's own soil ice for layer 1, at the point where it decides whether to
! evaluate the phase change at all.  Published for EVERY layer, called or not,
! because the guard itself is a claim a component must reproduce.
      ESM_FRZ_SICE(1) = SICE ; ESM_FRZ_SH2O_HRT(1) = SH2O (1)
