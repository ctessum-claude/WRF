! The melt rate BEFORE the pack-exhaustion limiter rewrites it as ESD/DT.
! EX is a rate and needs no differencing, but the limiter below is explicitly
! dt-dependent, so a reference needs to know whether it fired.
      ESM_SNO_EX_RAW = EX ; ESM_SNO_MELT_BLOCK = 1
