! The melt-water retention branch (TSNOWC >= 0): 13 % of the liquid water can
! be stored per day.  SNDENS here is the retained value BEFORE the 0.40 cap, so
! whether that cap fired is recoverable without a second flag.
      ESM_SNP_WET = 1 ; ESM_SNP_DW = DW ; ESM_SNP_SNDENS_WETRAW = SNDENS
